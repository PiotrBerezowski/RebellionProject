<Page x:Class="GUI.Delete"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Delete">

    <Grid Background="#802A2A">

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock FontSize="25" Text="Remove Rebel" Grid.Row="0" TextAlignment="Center" VerticalAlignment="Center" Foreground="DarkGray" />

        <Grid Grid.Row="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox Margin="10,10" />
            <Button Content="Annihilate" Margin="10,10,10,10" Grid.Row="0" Grid.Column="1" />
            <TextBox Margin="10,10" Grid.Row="1" Grid.Column="0" />
            <Button Content="Finish Off" Margin="10,10,10,10" Grid.Row="1" Grid.Column="1" />

        </Grid>

    </Grid>
</Page>
